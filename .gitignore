# Experiment state defaults to the operator's application-data directory.
# Keep the old/project-local escape hatch ignored if an operator explicitly
# chooses it; none of these mutable results are repository content.
/.foundry/
/output/
/tmp/

__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
.DS_Store
